id = "applet.nemosh.posixpath.no-operands"
area = "applet"
kind = "golden"
semantics = "nemosh"
platforms = ["windows", "linux", "darwin"]
references = ["busybox-w32", "nemosh"]
command = ["posixpath"]

[expect]
status = 1
stdout = ""
stderr = ""

[notes]
standard = "Nemosh Windows-native helper extension"
why = "posixpath fails when no operand is provided."
