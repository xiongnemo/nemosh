id = "applet.nemosh.posixpath.basic"
area = "applet"
kind = "golden"
semantics = "nemosh"
platforms = ["windows", "linux", "darwin"]
references = ["busybox-w32", "nemosh"]
command = ["posixpath", "C:/Users/nemo"]

[expect]
status = 0
stdout = "/c/Users/nemo\n"
stderr = ""

[notes]
standard = "Nemosh Windows-native helper extension"
why = "posixpath prints the canonical Nemosh spelling for a Windows drive path."
