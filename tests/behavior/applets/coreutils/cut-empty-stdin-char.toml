id = "applet.busybox-w32.cut.empty-stdin-char"
area = "applet"
kind = "golden"
semantics = "busybox-w32"
platforms = ["windows", "linux", "darwin"]
references = ["busybox-w32"]
command = ["cut", "-c", "1"]

[expect]
status = 0
stdout = ""
stderr = ""

[notes]
standard = "BusyBox-w32 cut utility"
why = "The first cut slice accepts a character list and produces no output for empty stdin."
