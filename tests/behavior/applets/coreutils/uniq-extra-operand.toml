id = "applet.busybox-w32.uniq.extra-operand"
area = "applet"
kind = "golden"
semantics = "busybox-w32"
platforms = ["windows", "linux", "darwin"]
references = ["busybox-w32"]
command = ["uniq", "first.txt", "second.txt"]

[expect]
status = 2
stdout = ""
stderr = "uniq: too many operands\n"

[notes]
standard = "BusyBox-w32 uniq utility"
why = "BusyBox-w32 supports an OUTFILE operand, but output-file support is intentionally deferred in the first Nemosh uniq slice."
