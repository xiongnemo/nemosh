id = "applet.busybox-w32.rm.smoke"
area = "applet"
kind = "golden"
semantics = "busybox-w32"
platforms = ["windows", "linux", "darwin"]
references = ["busybox-w32"]
script = "rm a.txt\nls\n"

[files]
"a.txt" = ""
"b.txt" = ""

[expect]
status = 0
stdout = "b.txt\n"
stderr = ""

[notes]
standard = "BusyBox-w32 coreutils/rm.c"
why = "rm unlinks the named file silently (coreutils/rm.c:60-70); ls confirms only the untouched file remains."
