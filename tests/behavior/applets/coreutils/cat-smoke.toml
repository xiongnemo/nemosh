id = "applet.busybox-w32.cat.smoke"
area = "applet"
kind = "golden"
semantics = "busybox-w32"
platforms = ["windows", "linux", "darwin"]
references = ["busybox-w32"]
script = "cat a.txt b.txt\n"

[files]
"a.txt" = "one\n"
"b.txt" = "two\n"

[expect]
status = 0
stdout = "one\ntwo\n"
stderr = ""

[notes]
standard = "BusyBox-w32 coreutils/cat.c"
why = "cat concatenates its operands in argument order with no separator (coreutils/cat.c:130-145)."
