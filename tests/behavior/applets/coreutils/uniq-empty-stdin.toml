id = "applet.busybox-w32.uniq.empty-stdin"
area = "applet"
kind = "golden"
semantics = "busybox-w32"
platforms = ["windows", "linux", "darwin"]
references = ["busybox-w32"]
command = ["uniq"]

[expect]
status = 0
stdout = ""
stderr = ""

[notes]
standard = "BusyBox-w32 uniq utility"
why = "The first uniq slice supports stdin; empty stdin is the behavior-runner smoke case."
