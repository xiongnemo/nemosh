id = "applet.busybox-w32.cut.delimiter-requires-field"
area = "applet"
kind = "golden"
semantics = "busybox-w32"
platforms = ["windows", "linux", "darwin"]
references = ["busybox-w32"]
command = ["cut", "-d", ":", "-c", "1"]

[expect]
status = 2
stdout = ""
stderr = "cut: -d DELIM requires -f\n"

[notes]
standard = "BusyBox-w32 cut utility"
why = "BusyBox-w32 only accepts a delimiter when field selection is active."
