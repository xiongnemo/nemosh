id = "applet.busybox-w32.cut.ignored-n-empty-stdin"
area = "applet"
kind = "golden"
semantics = "busybox-w32"
platforms = ["windows", "linux", "darwin"]
references = ["busybox-w32"]
command = ["cut", "-n", "-b", "1"]

[expect]
status = 0
stdout = ""
stderr = ""

[notes]
standard = "BusyBox-w32 cut utility"
why = "BusyBox-w32 accepts -n with byte mode, and empty stdin keeps the runner case feasible."
